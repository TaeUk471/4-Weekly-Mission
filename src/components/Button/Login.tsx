import styles from '../../styles/components/button/Login.module.css';

const Login = () => {
  return (
    <div className={styles.button}>
      <p className={styles.buttonText}>로그인</p>
    </div>
  );
};

export default Login;
